# 教师工作台 · 使用说明

## 目录结构

```
教师工作台/                      ← 父文件夹（整个文件夹可整体拷贝迁移）
├── app/                         ← 应用代码（更新/热补丁只动这里）
│   ├── www/                     ← 前端页面
│   ├── server.js / wb.js        ← 本地服务与命令行工具
│   ├── lib/                     ← 存储核心
│   └── config.json              ← 配置（appDir: www, dataDir: ../data）
├── data/                        ← ★ 你的数据（与代码完全隔离）
│   ├── records/                 ← 台账 JSON（学生/成绩/考勤/通知…每键一文件）
│   ├── attachments/             ← 附件
│   └── backup/                  ← 备份快照（自动生成）
└── 打开教师工作台.vbs           ← 双击启动应用
```

## 打开方式

- **双击**父文件夹内的 `打开教师工作台.vbs`，或桌面快捷方式「教师工作台」
- 服务地址：http://127.0.0.1:8731/

## 数据保护机制

1. **应用/数据隔离**：`app/` 与 `data/` 是两个独立子文件夹。
   - **热补丁更新**：只替换 `app/` 里的文件即可，`data/` 完全不受影响，录入数据永不丢失。
2. **导入自动快照**：每次「导入数据」（合并或替换）前，系统自动在 `data/backup/` 生成 `pre-import-时间戳.json` 快照——导入不满意可随时恢复。
3. **清空自动快照**：清空本机数据前同样自动生成 `pre-clear-时间戳.json`。
4. **写入原子化**：所有台账保存都是"先写临时文件再原子替换"，异常中断不会损坏数据。
5. **导入失败自动回滚**：导入内容损坏时自动恢复原数据。

## 备份与恢复

- **手动备份**：命令行执行 `app\wb.js backup` → 生成到 `data/backup/`
- **完整备份**：直接复制整个 `data/` 文件夹即可（包含全部台账与附件）
- **恢复**：
  - 从自动快照恢复：把 `data/backup/pre-import-xxx.json` 用 `app\wb.js import <文件> --replace` 导入
  - 或直接用快照文件替换 `data/records/` 下对应内容（需先停止服务）

## 迁移到另一台电脑

1. 复制整个「教师工作台」父文件夹到新电脑
2. 新电脑安装 Node.js（≥18）
3. 双击 `打开教师工作台.vbs` 即可，数据自动跟随

## 命令行工具（可选）

```
node app/wb.js status          查看状态
node app/wb.js backup          生成备份
node app/wb.js export <file>   导出完整备份
node app/wb.js import <file>   导入备份（默认合并，--replace 覆盖）
node app/wb.js start / stop    启停服务
```
